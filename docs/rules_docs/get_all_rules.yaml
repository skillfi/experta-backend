tags:
  - name: Rules
summary: Get all existing Rules
security:
  - Bearer: []
responses:
  200:
    description: Successful operation
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              meat:
                type: string
              action:
                type: string
              degreeOfReadiness:
                type: string
              time:
                type: integer
              turnedOver:
                type: boolean
              cookedFor:
                type: object
                properties:
                  name:
                    type: string
              addedOn:
                type: string
                format: date-time
  400:
    description: Rules not found